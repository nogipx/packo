projectDirectory: /Users/nogipx/Documents/Projects/nogipx/test_env
artifactsOutputsDirectory: /Users/nogipx/Documents/Projects/nogipx/packo/apk_builds
requiredEnv:
  - BUNDLE_ID_SUFFIX
  - APP_NAME
  - ENVIRONMENT
envFile: test.env
initialEnv:
  BUNDLE_ID_SUFFIX: "(ENVIRONMENT != null ? '.' + ENVIRONMENT : '')"
#  APP_NAME: "TestEnv"
  APP_NAME: "'TestEnv' + (BUILD_TYPE != 'release' ? '.' + BUILD_TYPE : '')"


projectDirectory: /Users/nogipx/Documents/Projects/nogipx/test_env
artifactsOutputsDirectory: /Users/nogipx/Documents/Projects/nogipx/packo/apk_builds
requiredEnv:
  - BUNDLE_ID_SUFFIX
  - APP_NAME
  - ENVIRONMENT
envFile: test.env
initialEnv:
  BUNDLE_ID_SUFFIX: "env.dot.ENVIRONMENT"
  APP_NAME: "'TestEnv' + (env.BUILD_TYPE != 'release' ? env.dot.BUILD_TYPE : '') + '-' + datetime.nowFull"

